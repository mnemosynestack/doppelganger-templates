## 2024-05-18 - Debounce Client-Side Search
**Learning:** In Next.js App Router applications, pushing to the router on every keystroke in a search input can cause excessive server-side data fetches and client-side navigations, significantly degrading performance.
**Action:** Always implement a debounce (e.g., using `setTimeout` and `useRef`) on input fields that trigger route changes or API calls. This batches rapid updates into a single execution after the user pauses typing.## 2024-05-24 - Database Query Optimization\n**Learning:** The database schema for presets contains large fields like `configuration` and `expected_output`. Fetching all columns via `SELECT *` for list views causes unnecessary bandwidth and memory overhead.\n**Action:** Always fetch only the required columns (e.g., id, title, description, icon) in API list endpoints to minimize overhead and improve response times.
